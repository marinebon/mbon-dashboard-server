This directory is used for custom airflow plugins.
Although we are not using any custom plugins at the time of writing this directory still needs to exist and so this README is here as a placeholder.
